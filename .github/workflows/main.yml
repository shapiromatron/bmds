name: CI

on: [push]

jobs:
  build:

    runs-on: python:3.6-alpine
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        pip install -U pip
        pip install -e .[test]
        py.test
